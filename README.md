This project demonstrates the implementation of a self-balancing robot that maintains upright stability using sensor fusion and closed-loop control. 
The robot uses an Inertial Measurement Unit (MPU6050) to detect tilt angle and employs a PID controller to drive two stepper motors for balance correction.
